Inside the directory called "frontend", all the code was created with ReactJ. 
Apart from this, inside the directory called "backend" there are two system versions:
*Single service proyect: the whole backend is contained in a single java service.
*Proyect splited in microservices: the backend is splited in microservices, each of them includes their own Dockerfile to be individually deployed in Docker.